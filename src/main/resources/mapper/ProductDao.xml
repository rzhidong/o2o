<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.o2o.dao.ProductDao">


	<insert id="insertProduct" useGeneratedKeys="true" keyProperty="productId"
		keyColumn="product_id" parameterType="com.o2o.entity.Product">
		INSERT INTO
		tb_product(
		product_name,
		product_desc,
		<if test="imgAddr != null">img_addr,</if>
		<if test="normalPrice != null">normal_price,</if>
		<if test="promotionPrice">promotion_price,</if>
		<if test="point != null">point,</if>
		priority,
		create_time,
		last_edit_time,
		enable_status,
		product_category_id,
		shop_id)
		VALUES(
		#{productName},
		#{productDesc},
		<if test="imgAddr != null">#{imgAddr},</if>
		<if test="normalPrice != null">#{normalPrice},</if>
		<if test="promotionPrice">#{promotionPrice},</if>
		<if test="point != null">#{point},</if>
		#{priority},
		#{createTime},
		#{lastEditTime},
		#{enableStatus},
		#{productCategory.productCategoryId},
		#{shop.shopId})
	</insert>

</mapper>
    